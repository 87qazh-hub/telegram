services:
  - type: web
    name: reel-downloader-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python reel_downloader_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
